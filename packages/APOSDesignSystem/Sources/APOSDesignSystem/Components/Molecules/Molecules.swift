// MARK: - Molecule Components
//
// Molecules are functional groupings of atoms that form reusable UI patterns.
//
// Implemented components:
// - IconLabel: SF Symbol + text, horizontal (IconLabel.swift)
// - MetricCard: Value + label + optional trend indicator (MetricCard.swift)
// - ActionRow: Icon + label + optional trailing content + disclosure (ActionRow.swift)
// - LabeledTextField: Label + AppTextField + validation message (LabeledTextField.swift)
// - FilterChip: Tag/chip for filter selections (FilterChip.swift)
// - EmptyAction: ContentUnavailableView wrapper with theme (EmptyAction.swift)
