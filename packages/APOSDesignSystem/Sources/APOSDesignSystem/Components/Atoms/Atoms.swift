// MARK: - Atom Components
//
// Atoms are the smallest UI building blocks — they cannot be decomposed further.
// They wrap SwiftUI primitives with design system enforcement.
//
// Implemented components:
// - AppText: Themed Text with semantic style (AppText.swift)
// - AppIcon: SF Symbol with theme tinting (AppIcon.swift)
// - AppButton: Primary, Secondary, Destructive, Ghost variants (AppButton.swift)
// - AppTextField: Themed text input with validation state (AppTextField.swift)
// - AppToggle: Themed toggle with label (AppToggle.swift)
// - AppDivider: Themed divider using border token (AppDivider.swift)
